apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: api-server
  namespace: default
spec:
  rules:
    - http:
        paths:
          - path: /
            backend:
              serviceName: api-server
              servicePort: 3000
